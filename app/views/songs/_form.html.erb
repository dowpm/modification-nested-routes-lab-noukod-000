<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <!-- <%= f.collection_select :artist, Artist.all, :id, :name %><br> -->
  <%= artist_select @song %><br>
  <!-- <%= f.label :artist_name %>
  <%= f.text_field :artist_name %> -->
  <br />

  <%= f.submit %>
<% end %>
